language: python
python:
  - "2.7"
script:
  - cd client
  - ENV=travis npm test
  - cd ../server
# command to install dependencies
# install: "pip install -r requirements.txt"
# command to run tests
# script: nosetests
